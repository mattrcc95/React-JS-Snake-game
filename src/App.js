import { Container } from "reactstrap";

const snakeGrid = {
  maxWidth: '70vw',
  minHeight: '75vh',
  display: 'grid',
  paddingTop: '10vh',
  paddingBottom: '10vh',
  margin: 'auto'
}

function App() {
  return (
    <Container style = {snakeGrid}>
      <div>merda</div>
    </Container>
  );
}

export default App;
